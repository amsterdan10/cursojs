let num = [1, 4, 7, 2, 0, 9, 2]

for (let pos = 0; pos < num.length; pos++) {
    console.log(`A posição ${pos} tem o valor ${num[pos]}`)
}

/*
    nomevariável.push(valor) > Adiciona o valor no final da Array.

    nomevariável.sort() > Deixar os valores da Array em ordem crescente.

    nomevariável.length > Exibe a quantidade de valores dentro da Array.
*/