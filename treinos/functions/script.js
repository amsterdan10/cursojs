let n1 = 12

function parimp(n) {
    if (n % 2 == 0) {
        return 'Par'
    } else {
        return 'Impar'
    }
}
/*
let res = parimp(n1)
console.log(`O número ${n1} é ${res}`)
*/
console.log(parimp(n1))